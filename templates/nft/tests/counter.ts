import * as anchor from "@coral-xyz/anchor";

describe("{{PROGRAM_NAME_TITLE}}", () => {
  it("builds", async () => {
    const provider = anchor.AnchorProvider.local();
    anchor.setProvider(provider);
  });
});

